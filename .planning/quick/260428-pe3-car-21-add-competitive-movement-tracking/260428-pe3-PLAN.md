---
phase: quick
plan: 260428-pe3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useVendorSwitchingIntel.ts
  - src/components/vendor-dashboard/CompetitiveMovementCard.tsx
  - src/components/vendor-dashboard/DashboardIntel.tsx
  - src/components/vendor-dashboard/DashboardDealerSignals.tsx
  - src/components/vendor-dashboard/VendorCommandCenter.tsx
autonomous: true
requirements: [CAR-21]
must_haves:
  truths:
    - "Market Intel section shows competitive movement (gained from / lost to) after the competitor comparison table"
    - "Dealer Signals section shows competitive flow data after exit reasons"
    - "Intelligence Hub shows a compact competitive movement summary in the Insights & Actions grid"
    - "All three views use the same shared hook and RPC call, not duplicated queries"
    - "Components gracefully handle empty/null data by rendering nothing"
  artifacts:
    - path: "src/hooks/useVendorSwitchingIntel.ts"
      provides: "Shared React Query hook wrapping get_vendor_switching_intel RPC"
      exports: ["useVendorSwitchingIntel"]
    - path: "src/components/vendor-dashboard/CompetitiveMovementCard.tsx"
      provides: "Reusable competitive movement card for vendor dashboard contexts"
      exports: ["CompetitiveMovementCard", "CompetitiveMovementCompact"]
  key_links:
    - from: "src/components/vendor-dashboard/DashboardIntel.tsx"
      to: "src/hooks/useVendorSwitchingIntel.ts"
      via: "useVendorSwitchingIntel(vendorName)"
      pattern: "useVendorSwitchingIntel"
    - from: "src/components/vendor-dashboard/DashboardDealerSignals.tsx"
      to: "src/hooks/useVendorSwitchingIntel.ts"
      via: "useVendorSwitchingIntel(vendorName)"
      pattern: "useVendorSwitchingIntel"
    - from: "src/components/vendor-dashboard/VendorCommandCenter.tsx"
      to: "src/hooks/useVendorSwitchingIntel.ts"
      via: "useVendorSwitchingIntel(vendorName)"
      pattern: "useVendorSwitchingIntel"
---

<objective>
Add competitive movement tracking (CAR-21) to the vendor dashboard across three sections: Market Intel, Dealer Signals, and Intelligence Hub.

Purpose: Vendors need to see which competitors they are gaining dealers from and losing dealers to, directly within their dashboard — not just on the member-facing profile page.

Output: A shared hook, two component variants (full card + compact summary), integrated into three existing dashboard sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/vendors/SwitchingIntel.tsx
@src/components/vendor-dashboard/DashboardIntel.tsx
@src/components/vendor-dashboard/DashboardDealerSignals.tsx
@src/components/vendor-dashboard/VendorCommandCenter.tsx
@src/hooks/useActiveProductLine.ts

<interfaces>
<!-- Existing RPC and types from SwitchingIntel.tsx -->

From src/components/vendors/SwitchingIntel.tsx:
```typescript
interface VendorCount {
  vendor: string;
  count: number;
}

interface SwitchingIntelResult {
  switched_to: number;
  switched_from: number;
  to_sources: VendorCount[];
  from_destinations: VendorCount[];
}

// RPC call pattern:
supabase.rpc("get_vendor_switching_intel" as never, { p_vendor_name: vendorName } as never)
```

From src/components/vendor-dashboard/DashboardIntel.tsx:
```typescript
interface DashboardIntelProps {
  vendorName: string;
}
// Uses useClerkSupabase() for authenticated client
// Uses useActiveProductLine() for product line context
```

From src/components/vendor-dashboard/DashboardDealerSignals.tsx:
```typescript
interface DashboardDealerSignalsProps {
  vendorName: string;
}
// Uses useVendorTechStackIntel(vendorName, productLineSlug)
```

From src/components/vendor-dashboard/VendorCommandCenter.tsx:
```typescript
interface VendorCommandCenterProps {
  vendorName: string;
}
// Uses useVendorIntelligenceDashboard(vendorName, productLineSlug)
// "Insights & Actions" grid at line 163: grid-cols-1 lg:grid-cols-2 gap-8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared hook and competitive movement components</name>
  <files>src/hooks/useVendorSwitchingIntel.ts, src/components/vendor-dashboard/CompetitiveMovementCard.tsx</files>
  <action>
**Hook — `src/hooks/useVendorSwitchingIntel.ts`:**
Create a React Query hook that wraps the existing `get_vendor_switching_intel` RPC. Pattern:
- Export `useVendorSwitchingIntel(vendorName: string)` returning `{ data, isLoading, isError }`
- Use `useClerkSupabase()` for the authenticated Supabase client (same pattern as DashboardIntel)
- Query key: `["vendor-switching-intel", vendorName]`
- `enabled: !!vendorName`
- `staleTime: 5 * 60 * 1000` (matches existing caching patterns)
- Return type: `SwitchingIntelResult | null`
- Export the `SwitchingIntelResult` and `VendorCount` interfaces from this file so components can use them

**Components — `src/components/vendor-dashboard/CompetitiveMovementCard.tsx`:**
Create TWO exported components in one file:

1. `CompetitiveMovementCard` (full view for Market Intel and Dealer Signals):
   - Props: `vendorName: string`
   - Calls `useVendorSwitchingIntel(vendorName)`
   - Returns `null` if loading, error, or no data (switched_to + switched_from === 0)
   - Layout: rounded-xl border bg-white p-5 (matching DashboardIntel card style)
   - Header with ArrowRightLeft icon + "Competitive Movement" title + NetTrendBadge (reuse the logic from SwitchingIntel.tsx — inline the NetTrendBadge as a local component, do NOT import from SwitchingIntel since that's member-facing)
   - Two-column grid (sm:grid-cols-2) showing "Gained from" (emerald) and "Lost to" (red) lists
   - Style rows as rounded-lg bg-emerald-50/50 border-emerald-100 (and red equivalent) with vendor name + count — same visual pattern as SwitchingIntel.tsx
   - Max 5 items per column (slice to 5)

2. `CompetitiveMovementCompact` (summary for Intelligence Hub):
   - Props: `vendorName: string`
   - Calls `useVendorSwitchingIntel(vendorName)`
   - Returns `null` if no data
   - Layout: rounded-2xl border border-slate-200 bg-white shadow-sm p-6 h-full (matching VendorCommandCenter card style)
   - Header: "Competitive Movement" with ArrowRightLeft icon
   - Show net trend badge prominently
   - Two stats: "Gained: X dealers" (emerald) and "Lost: X dealers" (red) as compact stat rows
   - Show top 3 vendors in each direction as small text list (truncated)
   - No loading spinner — just return null while loading (keeps Intelligence Hub layout clean)
  </action>
  <verify>
    <automated>cd /Users/miguel/Pulse/Pulse && npx tsc --noEmit src/hooks/useVendorSwitchingIntel.ts src/components/vendor-dashboard/CompetitiveMovementCard.tsx 2>&1 | head -20 || npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Hook exports useVendorSwitchingIntel with correct typing. CompetitiveMovementCard and CompetitiveMovementCompact render competitive movement data with gained/lost columns. Both return null when no data available.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate competitive movement into all three dashboard sections</name>
  <files>src/components/vendor-dashboard/DashboardIntel.tsx, src/components/vendor-dashboard/DashboardDealerSignals.tsx, src/components/vendor-dashboard/VendorCommandCenter.tsx</files>
  <action>
**DashboardIntel.tsx (Market Intel):**
- Import `CompetitiveMovementCard` from `@/components/vendor-dashboard/CompetitiveMovementCard`
- Add `<CompetitiveMovementCard vendorName={vendorName} />` AFTER the "Competitor Comparison" table div (after the closing `</div>` at approximately line 262), wrapped in a `<div className="mt-6">` container
- No other changes to existing code

**DashboardDealerSignals.tsx (Dealer Signals):**
- Import `CompetitiveMovementCard` from `@/components/vendor-dashboard/CompetitiveMovementCard`
- Add `<CompetitiveMovementCard vendorName={vendorName} />` AFTER the Exit Reasons section and BEFORE the Market Share section (between lines ~149 and ~152 in the current layout)
- The component self-handles loading/empty states so no conditional wrapper needed
- Only render if `!data.below_threshold` (it's already inside that branch of the return)

**VendorCommandCenter.tsx (Intelligence Hub):**
- Import `CompetitiveMovementCompact` from `@/components/vendor-dashboard/CompetitiveMovementCard`
- In the "Insights & Actions Section" grid (line ~163), change from `grid-cols-1 lg:grid-cols-2` to `grid-cols-1 lg:grid-cols-3` to accommodate a third column
- Add the CompetitiveMovementCompact as a third item in the grid:
  ```tsx
  {/* Competitive Movement */}
  <div className="space-y-4">
    <h2 className="text-sm font-bold text-slate-900">Competitive Movement</h2>
    <CompetitiveMovementCompact vendorName={vendorName} />
  </div>
  ```
- Place it after the "Historical Momentum" div (as the third grid child)
  </action>
  <verify>
    <automated>cd /Users/miguel/Pulse/Pulse && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All three dashboard sections render competitive movement data. Market Intel shows full card after competitor table. Dealer Signals shows full card after exit reasons. Intelligence Hub shows compact summary in a 3-column insights grid. Build passes with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RPC | Frontend calls get_vendor_switching_intel — RLS enforces vendor can only see their own data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I (Information Disclosure) | useVendorSwitchingIntel | accept | RPC already enforced by existing RLS policies; vendor can only query their own name. No new data exposure — same RPC already called by member-facing SwitchingIntel.tsx |
| T-quick-02 | S (Spoofing) | vendorName prop | accept | vendorName flows from authenticated vendor session, not user input. Existing pattern across all dashboard components. |
</threat_model>

<verification>
1. `npm run build` passes with zero errors
2. Navigate to vendor dashboard Market Intel tab — competitive movement card appears below competitor comparison table (or nothing if no switching data)
3. Navigate to Dealer Signals tab — competitive movement card appears after exit reasons section
4. Navigate to Intelligence Hub tab — compact competitive movement appears in insights grid as third column
5. All three views show consistent data (same gained/lost counts) since they share the same hook + query key
</verification>

<success_criteria>
- Shared hook `useVendorSwitchingIntel` eliminates RPC call duplication
- CompetitiveMovementCard renders full gained/lost columns with vendor lists
- CompetitiveMovementCompact renders summary stats for Intelligence Hub
- All three dashboard sections integrate the appropriate component variant
- Components return null gracefully when no competitive movement data exists
- Build succeeds with no TypeScript or bundler errors
</success_criteria>

<output>
After completion, create `.planning/quick/260428-pe3-car-21-add-competitive-movement-tracking/260428-pe3-SUMMARY.md`
</output>
