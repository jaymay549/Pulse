---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/admin/AdminSidebar.tsx
  - src/hooks/useAdminSidebarConfig.ts
autonomous: true
must_haves:
  truths:
    - "Admin sidebar shows two distinct sections: Active (top) and Unused (bottom)"
    - "Unused section is collapsed by default and toggleable via chevron"
    - "Send Message, Prompts, Trends, Debug default to Unused section"
    - "Admins can drag items between Active and Unused sections"
    - "Sidebar configuration persists across page reloads per Clerk user ID"
    - "Navigation still works — clicking any item navigates to the correct route"
  artifacts:
    - path: "src/hooks/useAdminSidebarConfig.ts"
      provides: "Sidebar config persistence hook with localStorage keyed by Clerk user ID"
    - path: "src/components/admin/AdminSidebar.tsx"
      provides: "Refactored sidebar with Active/Unused sections, DnD, collapsible Unused"
  key_links:
    - from: "src/components/admin/AdminSidebar.tsx"
      to: "src/hooks/useAdminSidebarConfig.ts"
      via: "hook import"
      pattern: "useAdminSidebarConfig"
    - from: "src/hooks/useAdminSidebarConfig.ts"
      to: "localStorage"
      via: "getItem/setItem with Clerk userId key"
      pattern: "localStorage\\.(get|set)Item"
---

<objective>
CAR-29: Reorganize AdminSidebar into Active/Unused collapsible sections with drag-and-drop reordering and per-user localStorage persistence.

Purpose: The admin panel has 17 nav items, many unused. This declutters the sidebar by letting admins hide items they don't use while keeping them accessible in a collapsible section.
Output: Refactored AdminSidebar with DnD support, collapsible Unused section, and a persistence hook.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/admin/AdminSidebar.tsx
@src/components/admin/AdminLayout.tsx
@src/hooks/useClerkAuth.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dnd-kit and create sidebar config persistence hook</name>
  <files>package.json, package-lock.json, src/hooks/useAdminSidebarConfig.ts</files>
  <action>
1. Install dependencies: `npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`

2. Create `src/hooks/useAdminSidebarConfig.ts` with the following:

- Define `NAV_ITEMS` array (moved from AdminSidebar.tsx) with all 17 items. Each item: `{ id: string, to: string, icon: LucideIcon, label: string, end?: boolean }`. The `id` should be the route segment (e.g., "dashboard", "sales-targets", "vendors", etc.).

- Define `DEFAULT_UNUSED_IDS`: `["send", "prompts", "trends", "debug"]` — these are the items that default to the Unused section.

- Export `useAdminSidebarConfig(userId: string | undefined)` hook that:
  - Stores `{ activeIds: string[], unusedIds: string[] }` in state
  - On mount, reads from `localStorage` key `admin-sidebar-config-${userId}` (if userId exists)
  - If no stored config or no userId, derives default: all item IDs except DEFAULT_UNUSED_IDS go to activeIds, DEFAULT_UNUSED_IDS go to unusedIds
  - Exposes `activeItems` and `unusedItems` (resolved NavItem objects, in order)
  - Exposes `moveItem(itemId: string, toSection: "active" | "unused", newIndex?: number)` that updates state and persists to localStorage
  - Exposes `reorderSection(section: "active" | "unused", orderedIds: string[])` for within-section reorder + persist
  - Any new nav items not in stored config get appended to activeIds (forward-compatible)
  </action>
  <verify>
    <automated>cd /Users/miguel/Pulse && npx tsc --noEmit src/hooks/useAdminSidebarConfig.ts 2>&1 | head -20</automated>
  </verify>
  <done>dnd-kit packages installed. useAdminSidebarConfig hook exists, exports activeItems/unusedItems/moveItem/reorderSection, compiles without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AdminSidebar with Active/Unused sections and drag-and-drop</name>
  <files>src/components/admin/AdminSidebar.tsx</files>
  <action>
Rewrite AdminSidebar.tsx:

1. Import `useAdminSidebarConfig` from the new hook. Import `useClerkAuth` to get userId. Import dnd-kit:
   - `DndContext`, `closestCenter`, `DragOverlay`, `DragStartEvent`, `DragEndEvent`, `DragOverEvent` from `@dnd-kit/core`
   - `SortableContext`, `verticalListSortingStrategy`, `useSortable` from `@dnd-kit/sortable`
   - `CSS` from `@dnd-kit/utilities`
   - `ChevronDown` from lucide-react (for collapsible toggle)

2. Create `SortableNavItem` component:
   - Uses `useSortable({ id })` hook
   - Renders a NavLink wrapped in a div with sortable transform/transition styles
   - Applies the same NavLink className pattern as current code (isActive highlight)
   - The drag handle is the entire item (no separate grip icon needed)

3. Refactor `AdminSidebar` component:
   - Call `const { user } = useClerkAuth()` and `const { activeItems, unusedItems, moveItem, reorderSection } = useAdminSidebarConfig(user?.id)`
   - Add `const [unusedCollapsed, setUnusedCollapsed] = useState(true)` for collapsible state
   - Wrap nav in `DndContext` with `closestCenter` collision strategy
   - Two droppable areas identified by `SortableContext`:
     - "Active" section: renders activeItems as SortableNavItem list
     - "Unused" section: header row with "Unused" label + ChevronDown icon (rotates when expanded), click toggles unusedCollapsed. When expanded, renders unusedItems as SortableNavItem list. Unused items should have slightly dimmer text (text-zinc-500 instead of text-zinc-400).
   - `onDragEnd` handler: determine source/destination section from the over target. If item moved between sections, call `moveItem()`. If reordered within same section, call `reorderSection()`.
   - Use `DragOverlay` for a styled drag preview (clone of the nav item being dragged)
   - Keep the "Back to Pulse" link at the bottom outside the DnD context (it is not draggable)

4. Unused section visual treatment:
   - Separator line (border-t border-zinc-800) between Active and Unused
   - "Unused" header: `text-xs uppercase tracking-wider text-zinc-600 px-3 py-2 flex items-center justify-between cursor-pointer`
   - ChevronDown rotates 180deg when expanded (use Tailwind `rotate-180` + transition)
   - When collapsed, the Unused items are hidden (not rendered)

5. Preserve the existing aside structure: same width (w-56), same border, same header ("CDG Admin"), same footer ("Back to Pulse"). Only the nav section changes.
  </action>
  <verify>
    <automated>cd /Users/miguel/Pulse && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>AdminSidebar renders Active/Unused sections. Unused section collapsed by default with chevron toggle. Items are draggable between sections. Config persists in localStorage per user. Build succeeds with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | Sidebar config stored client-side, admin-only feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-car29-01 | T (Tampering) | localStorage config | accept | Config is cosmetic (nav item ordering), no security impact if tampered. All routes remain protected by AdminGuard regardless of sidebar config. |
| T-car29-02 | I (Info Disclosure) | localStorage key includes userId | accept | userId is already available client-side via Clerk; no sensitive data in sidebar config. |
</threat_model>

<verification>
1. `npm run build` succeeds
2. Dev server: sidebar shows Active section with 13 items, Unused section (collapsed) with 4 items (Send Message, Prompts, Trends, Debug)
3. Clicking chevron expands/collapses Unused section
4. Dragging an item from Active to Unused moves it; vice versa
5. Refreshing page preserves the configuration
6. Navigation still works for all items in both sections
</verification>

<success_criteria>
- AdminSidebar has two sections: Active (top) and Unused (bottom, collapsible)
- Default unused items: Send Message, Prompts, Trends, Debug
- Drag-and-drop works between sections and for reordering within sections
- Configuration persists in localStorage keyed by Clerk user ID
- Build passes, no TypeScript errors
- AdminLayout requires no changes (AdminSidebar interface unchanged)
</success_criteria>

<output>
After completion, create `.planning/quick/260416-o1s-car-29-admin-panel-cleanup-collapsible-s/260416-o1s-SUMMARY.md`
</output>
