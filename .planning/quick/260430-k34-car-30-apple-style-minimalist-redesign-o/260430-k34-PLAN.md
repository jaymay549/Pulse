---
phase: quick-260430-k34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/VendorsV2.tsx
  - src/components/ui/smart-search-bar.tsx
  - src/components/vendors/UpgradePromptCard.tsx
autonomous: true
requirements: [CAR-30]
must_haves:
  truths:
    - "Landing hero has a subtle amber radial gradient background"
    - "5 suggested prompt chips appear on landing state (up from 3)"
    - "Search bar placeholder rotates through example prompts"
    - "Free users see inline upgrade CTA in the no-results dropdown instead of a separate UpgradePromptCard"
  artifacts:
    - path: "src/pages/VendorsV2.tsx"
      provides: "Gradient bg, expanded prompts, rotating placeholder, removal of UpgradePromptCard usage"
    - path: "src/components/ui/smart-search-bar.tsx"
      provides: "Rotating placeholder animation, inline upgrade CTA in no-results dropdown"
  key_links:
    - from: "src/pages/VendorsV2.tsx"
      to: "src/components/ui/smart-search-bar.tsx"
      via: "SmartSearchBar props (placeholder removed, new onUpgrade prop)"
      pattern: "SmartSearchBar"
---

<objective>
CAR-30: Apple-style minimalist redesign of VendorsV2 landing page.

Purpose: Make the vendor intel landing page feel premium and Apple-inspired with a gradient hero, more suggested prompts, rotating placeholder text, and inline upgrade messaging that replaces the separate UpgradePromptCard.

Output: Updated VendorsV2.tsx and SmartSearchBar with gradient, expanded prompts, rotating placeholder, and inline upgrade CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/VendorsV2.tsx
@src/components/ui/smart-search-bar.tsx
@src/components/vendors/UpgradePromptCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance SmartSearchBar with rotating placeholder and inline upgrade CTA</name>
  <files>src/components/ui/smart-search-bar.tsx</files>
  <action>
Two changes to SmartSearchBar:

**1. Rotating placeholder text:**
- Add a new optional prop `placeholders?: string[]` to SmartSearchBarProps.
- When `placeholders` is provided and input is empty + not focused, cycle through the array every 3 seconds with a subtle fade transition using Framer Motion's AnimatePresence.
- Use a `useState` for the current placeholder index, and a `useEffect` with `setInterval` that increments the index (mod array length). Clear interval when focused or when searchQuery is non-empty.
- Render the rotating text as an absolutely-positioned animated span overlaying the input (pointer-events-none), hidden when input is focused or has text. The actual input `placeholder` attribute should be empty when `placeholders` is provided (so the custom animation takes over).

**2. Inline upgrade CTA in no-results dropdown (lines 289-308):**
- Add a new optional prop `onUpgrade?: () => void` to SmartSearchBarProps.
- In the "No results" dropdown (the `AnimatePresence` block at lines 289-308), when `!isPro`, replace the plain text "AI search is a Pro feature" with a richer inline CTA:
  - Sparkles icon (already imported) + "AI Vendor Advisor" label in amber/primary color
  - One-line description: "Get instant answers about vendors, comparisons, and recommendations."
  - A small "Upgrade to Pro" button (using the imported Button from shadcn if needed, or a styled button element) that calls `onUpgrade?.()`.
  - Keep the overall dropdown style (white bg, rounded-xl, shadow-xl, border) but add slightly more vertical padding (p-5).
- When `isPro`, keep existing behavior: "press Enter to ask AI".
  </action>
  <verify>
    <automated>cd /Users/miguel/Pulse/Pulse && npx tsc --noEmit src/components/ui/smart-search-bar.tsx 2>&1 | head -20; echo "---"; grep -c "placeholders" src/components/ui/smart-search-bar.tsx; grep -c "onUpgrade" src/components/ui/smart-search-bar.tsx</automated>
  </verify>
  <done>SmartSearchBar accepts `placeholders` array prop and cycles through them with fade animation. No-results dropdown shows inline upgrade CTA with Sparkles icon and "Upgrade to Pro" button for free users.</done>
</task>

<task type="auto">
  <name>Task 2: Apply gradient hero, expand prompts, wire new SmartSearchBar props, remove UpgradePromptCard</name>
  <files>src/pages/VendorsV2.tsx, src/components/vendors/UpgradePromptCard.tsx</files>
  <action>
**1. Gradient background on landing hero (lines 1204-1223):**
- On the hero wrapper div (line 1205: `<div className="max-w-3xl mx-auto text-center pt-16..."`), add an inline style or a parent wrapper with the gradient.
- Add a full-width gradient wrapper AROUND the entire landing content area. Place it just inside the `isLandingState` conditional (line 1204). Use:
  ```
  style={{ background: 'radial-gradient(125% 125% at 50% 10%, #ffffff 40%, #f59e0b 100%)' }}
  ```
- Apply this to a div that wraps from the hero text down through the search bar area. Add `className="rounded-3xl -mx-4 px-4 sm:-mx-6 sm:px-6 lg:-mx-8 lg:px-8 pb-8"` so it bleeds edge-to-edge but keeps content padded. Use opacity at 15-20% for subtlety — adjust the gradient to: `radial-gradient(125% 125% at 50% 10%, transparent 40%, rgba(245, 158, 11, 0.15) 100%)`.

**2. Expand SUGGESTED_PROMPTS (line 46-50):**
Replace the array with 5 dealer-intent prompts:
```typescript
const SUGGESTED_PROMPTS = [
  "What DMS should I use for a mid-size dealership?",
  "Which vendors have the most dealer complaints?",
  "Best CRM for customer follow-up and retention?",
  "Compare F&I menu providers — pros and cons",
  "Who handles service scheduling well for multi-rooftop?",
];
```

**3. Wire rotating placeholder to SmartSearchBar (line 1257-1266):**
- Remove the static `placeholder` prop from SmartSearchBar.
- Add `placeholders={SUGGESTED_PROMPTS}` prop so the search bar cycles through the 5 prompts.
- Add `onUpgrade={() => setShowUpgradeModal(true)}` prop.

**4. Remove UpgradePromptCard usage (lines 1317-1324):**
- Delete the `{showUpgradePrompt && <UpgradePromptCard ... />}` block entirely (lines 1317-1324).
- Remove the `showUpgradePrompt` state variable (line 96) and `setShowUpgradePrompt(true)` call (line 964). Instead, at line 964 where `setShowUpgradePrompt(true)` was called, do nothing extra — the SmartSearchBar now handles the inline upgrade CTA automatically in its no-results dropdown.
- Remove the import of `UpgradePromptCard` from the imports section.
- Do NOT delete the UpgradePromptCard.tsx file itself (it may be used elsewhere or useful later), but verify it has no other imports. If it's only imported in VendorsV2.tsx, add a comment at the top: `// NOTE: No longer used on VendorsV2 landing. Kept for potential reuse.`
  </action>
  <verify>
    <automated>cd /Users/miguel/Pulse/Pulse && npx tsc --noEmit src/pages/VendorsV2.tsx 2>&1 | head -20; echo "---"; grep -c "SUGGESTED_PROMPTS" src/pages/VendorsV2.tsx; grep -c "UpgradePromptCard" src/pages/VendorsV2.tsx; grep -c "radial-gradient" src/pages/VendorsV2.tsx; grep -c "placeholders=" src/pages/VendorsV2.tsx</automated>
  </verify>
  <done>
- Hero section has subtle amber radial gradient background
- SUGGESTED_PROMPTS has 5 dealer-intent prompts
- SmartSearchBar receives `placeholders` and `onUpgrade` props
- UpgradePromptCard import and usage removed from VendorsV2
- `showUpgradePrompt` state removed
- Build compiles without type errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a purely cosmetic/UX change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-car30-01 | I (Information Disclosure) | Upgrade CTA text | accept | No sensitive data exposed; upgrade CTA is intentionally visible to free users |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. Landing page shows amber gradient background behind hero
3. 5 prompt chips visible below search bar
4. Search bar placeholder text rotates through prompts with fade animation
5. Free user typing a non-vendor query sees inline upgrade CTA in dropdown (not a separate card)
6. Pro user typing a non-vendor query sees "press Enter to ask AI" in dropdown
</verification>

<success_criteria>
- Landing page has Apple-style gradient hero with amber accent
- 5 suggested prompts shown as chips and rotating in search placeholder
- Inline upgrade messaging replaces UpgradePromptCard for free users
- No TypeScript errors, build passes
</success_criteria>

<output>
After completion, create `.planning/quick/260430-k34-car-30-apple-style-minimalist-redesign-o/260430-k34-SUMMARY.md`
</output>
