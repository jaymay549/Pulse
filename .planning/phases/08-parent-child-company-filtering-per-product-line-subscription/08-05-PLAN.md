---
phase: 08-parent-child-company-filtering-per-product-line-subscription
plan: 05
type: execute
wave: 1
depends_on: ["08-01"]
files_modified: []
autonomous: false
requirements:
  - PROD-01
  - PROD-14
must_haves:
  truths:
    - "Database schema is up to date with vendor_product_subscriptions table and all 5 SECURITY DEFINER functions"
  artifacts: []
  key_links: []
---

<objective>
Push database migrations to the live Supabase instance after Plan 01 creates the migration files. This is a BLOCKING step — the admin UI (Plan 02) and vendor dashboard (Plans 03-04) depend on these tables and functions existing in the database.

Purpose: Migration files only exist locally until pushed. Without this step, RPCs will fail with "function does not exist" errors.
Output: Live database updated with vendor_product_subscriptions table and helper functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-parent-child-company-filtering-per-product-line-subscription/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Push database migrations to Supabase</name>
  <read_first>
    supabase/migrations/20260423000000_vendor_product_subscriptions.sql
    supabase/migrations/20260423100000_vendor_product_tier_functions.sql
  </read_first>
  <action>
Push the two new migration files to the live Supabase database.

Run: `supabase db push`

If the command requires interactive confirmation, the user must run it manually. If `SUPABASE_ACCESS_TOKEN` env var is set, non-interactive push may work.

After push succeeds, verify the table and functions exist by running:
```sql
SELECT count(*) FROM information_schema.tables WHERE table_name = 'vendor_product_subscriptions';
SELECT routine_name FROM information_schema.routines WHERE routine_name IN ('vendor_product_tier', 'vendor_subscribed_slugs', 'admin_upsert_product_subscription', 'admin_delete_product_subscription', 'admin_list_product_subscriptions');
```
  </action>
  <how-to-verify>
    1. Run `supabase db push` from project root
    2. Confirm both migrations apply cleanly (no errors)
    3. Check Supabase dashboard: Table Editor should show `vendor_product_subscriptions` table
    4. Check Supabase dashboard: SQL Editor -> run `SELECT routine_name FROM information_schema.routines WHERE routine_name LIKE 'vendor_product%' OR routine_name LIKE 'admin_%_product%';` -> should return 5 rows
  </how-to-verify>
  <resume-signal>Type "pushed" when migrations are applied successfully</resume-signal>
  <acceptance_criteria>
    - `vendor_product_subscriptions` table exists in live database
    - `vendor_product_tier` function exists
    - `vendor_subscribed_slugs` function exists
    - `admin_upsert_product_subscription` function exists
    - `admin_delete_product_subscription` function exists
    - `admin_list_product_subscriptions` function exists
  </acceptance_criteria>
  <done>All Phase 8 migrations applied to live Supabase database. Tables and functions ready for use by frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration push -> live DB | Schema changes applied to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Denial of Service | Migration push | accept | Standard Supabase migration push; table is empty and new — no data loss risk. Functions are CREATE OR REPLACE — idempotent. |
</threat_model>

<verification>
- `supabase db push` completes without errors
- Supabase dashboard shows vendor_product_subscriptions table
</verification>

<success_criteria>
- Live database has the new table and all 5 functions
- Wave 2 plans (02, 03) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/08-parent-child-company-filtering-per-product-line-subscription/08-05-SUMMARY.md`
</output>
